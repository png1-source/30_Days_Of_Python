
length_of_rect = int(input('Enter the length of the recatangle: ')) # Gets the length of the rectangle through the int((input)) function
width_of_rect = int(input('Enter the width of the rectangle: ')) # Gets the width of the rectangle through the int((input)) function

rectangle_area = length_of_rect * width_of_rect # Calculates the area of the rectangle 

print(f'The area of the rectangle is {rectangle_area}') # Prints the desired output 